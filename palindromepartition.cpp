#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    vector<vector<string> > partition(string s) {
        std::vector<std::vector<string> > v;
		for(size_t i = i; i < s.size(); ++i)
		{
			string
		}
    }
};

int main (int argc, char const *argv[])
{
	Solution s;
	vector<vector<string> > v = s.partition("aab");
	for(size_t i = 0; i < v.size(); ++i)
	{
		for(size_t j = 0; j < v[i].size(); ++j)
		{
			cout << v[i][j] << ",";
		}
		cout << endl;
	}
	return 0;
}